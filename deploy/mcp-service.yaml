apiVersion: v1
kind: Service
metadata:
  name: coder-k8s
  namespace: coder-system
spec:
  selector:
    app: coder-k8s
  ports:
    - name: mcp
      port: 8090
      protocol: TCP
      targetPort: 8090
